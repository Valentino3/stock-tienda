// Nombre genérico de la plataforma (pre-login y título). La marca de cada
// comercio ahora es el nombre de su TIENDA (stores.name), no una constante
// global — se resuelve por sesión y se pasa al shell.
export const APP_NAME = "Stock Tienda";
